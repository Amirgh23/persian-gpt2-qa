# گزارش تمرین: طراحی سامانه پرسش‌وپاسخ با GPT‑2

## ۱. عنوان تمرین

طراحی و پیاده‌سازی یک برنامه پرسش‌وپاسخ با استفاده از مدل زبانی ازپیش‌آموزش‌دیده GPT‑2

## ۲. هدف تمرین

هدف این تمرین آشنایی با مدل‌های زبانی ازپیش‌آموزش‌دیده و استفاده از آن‌ها برای تولید پاسخ
به پرسش‌های کاربر است. در این پروژه، سؤال کاربر همراه با یک متن مرجع اختیاری به مدل داده
می‌شود و مدل ادامه متن را به‌عنوان پاسخ تولید می‌کند.

## ۳. ابزارها و کتابخانه‌های استفاده‌شده

- زبان برنامه‌نویسی Python
- کتابخانه Transformers برای بارگذاری توکنایزر و مدل
- کتابخانه PyTorch برای اجرای مدل
- مدل `HooshvareLab/gpt2-fa` به‌عنوان نسخه فارسی مبتنی بر معماری GPT‑2
- کتابخانه unittest برای آزمون بخش‌های مستقل برنامه

## ۴. روش انجام تمرین

ابتدا مدل و توکنایزر با کلاس‌های `AutoTokenizer` و `AutoModelForCausalLM` بارگذاری
می‌شوند. برنامه سؤال و متن مرجع را در قالب زیر قرار می‌دهد:

```text
متن مرجع: ...
سؤال: ...
پاسخ کوتاه و دقیق:
```

سپس پرامپت توکن‌سازی و به مدل ارسال می‌شود. مدل با استفاده از روش‌های نمونه‌گیری Top-k و
Top-p پاسخ را تولید می‌کند. برای کاهش تکرار، از `repetition_penalty` و
`no_repeat_ngram_size` استفاده شده است. در پایان، توکن‌های مربوط به پرامپت از خروجی جدا
و فقط متن پاسخ نمایش داده می‌شود.

برنامه دو حالت اجرا دارد:

1. حالت تعاملی که در آن می‌توان چند سؤال متوالی پرسید.
2. حالت تک‌سؤال که سؤال و متن مرجع از طریق آرگومان‌های خط فرمان دریافت می‌شوند.

## ۵. ساختار فایل‌ها

- `app.py`: کد اصلی بارگذاری مدل و تولید پاسخ
- `test_app.py`: آزمون‌های خودکار پروژه
- `requirements.txt`: وابستگی‌های لازم
- `README.md`: راهنمای نصب و اجرا
- `REPORT.md`: گزارش و نتیجه تمرین

## ۶. نحوه نصب و اجرا

در محیط PowerShell وارد پوشه پروژه شوید و فرمان‌های زیر را اجرا کنید:

```powershell
python -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt
python app.py
```

نمونه اجرای مستقیم یک سؤال:

```powershell
python app.py --question "پایتخت ایران کجاست؟" --context "تهران پایتخت ایران است."
```

برای اجرای آزمون‌ها:

```powershell
python -m unittest -v
```

## ۷. نتایج آزمون

سه آزمون خودکار زیر اجرا و همگی با موفقیت گذرانده شدند:

| آزمون | نتیجه |
|---|---|
| ساخت صحیح پرامپت همراه متن مرجع | موفق |
| جلوگیری از پذیرش سؤال خالی | موفق |
| حذف بخش سؤال بعدی از پاسخ تولیدشده | موفق |

خلاصه خروجی آزمون:

```text
Ran 3 tests in 0.001s
OK
```

همچنین فایل اصلی با `py_compile` بررسی شد و هیچ خطای نحوی در آن مشاهده نشد.

اجرای نخست مدل کامل نیازمند نصب وابستگی‌ها و دانلود وزن‌های مدل از Hugging Face است.
به همین علت، نتیجه آزمون‌های بالا مربوط به منطق قطعی برنامه است و کیفیت محتوای پاسخ مدل
ممکن است در اجراهای مختلف تغییر کند.

## ۸. بررسی نتیجه

پروژه نشان می‌دهد که یک مدل زبانی GPT‑2 را می‌توان با طراحی پرامپت برای یک سامانه ساده
پرسش‌وپاسخ به کار برد. وجود متن مرجع، مدل را به سمت پاسخ مرتبط‌تر هدایت می‌کند. بااین‌حال،
GPT‑2 اساساً برای ادامه‌دادن متن آموزش دیده است و یک مدل تخصصی استخراج پاسخ نیست؛ بنابراین
ممکن است پاسخ ناقص، نادرست یا ساختگی تولید کند.

## ۹. پیشنهاد برای بهبود

- تنظیم دقیق مدل روی مجموعه‌داده پرسش‌وپاسخ فارسی
- استفاده از روش بازیابی متن و قراردادن بخش مرتبط در پرامپت
- افزودن رابط گرافیکی یا وب
- ارزیابی مدل با معیارهایی مانند Exact Match و F1
- مقایسه نتیجه با مدل‌های جدیدتر و تخصصی‌تر پرسش‌وپاسخ

## ۱۰. جمع‌بندی

در این تمرین یک برنامه قابل‌اجرا برای پرسش‌وپاسخ فارسی با مدل ازپیش‌آموزش‌دیده مبتنی بر
GPT‑2 ساخته شد. برنامه سؤال و متن مرجع را دریافت، آن‌ها را به پرامپت تبدیل و پاسخ را تولید
می‌کند. آزمون‌های منطق برنامه با موفقیت انجام شدند. نتیجه کلی این است که GPT‑2 برای نمایش
مفهوم پرسش‌وپاسخ مولد مناسب است، اما برای کاربرد دقیق و واقعی به تنظیم دقیق، داده مناسب و
سازوکار ارزیابی نیاز دارد.
